require "test_helper"

describe MachinesController do
  # it "must get create" do
  #   post machines_url()
  #   must_respond_with :success
  # end

  # it "must put stop" do
  #   put stop_machine_url(id: 'foo')
  #   must_respond_with :success
  # end

  # it "must put start" do
  #   put start_machine_url(id: 'foo')
  #   must_respond_with :success
  # end

  # it "must delete destroy" do
  #   delete machine_url(id: 'foo')
  #   must_respond_with :success
  # end

  # it "must get show" do
  #   get machines_url(id: 'foo')
  #   must_respond_with :success
  # end

end
